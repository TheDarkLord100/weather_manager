name: weather_manager
description: "A Flutter package to manage and fetch weather data using the OpenWeather API."
version: 1.0.0
homepage: https://github.com/TheDarkLord100/weather_managerdsa

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16